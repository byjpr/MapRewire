# MapRewire

Syntactic sugar to simply bulk rekey maps

```elixir
def deps do
  [
    {:map_rewire, "~> 0.1.0"}
  ]
end
```

Want to see inside MapRewire?

```elixir
config :map_rewire,
  debug?: true
```
